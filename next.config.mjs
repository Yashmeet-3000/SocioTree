/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'wallpapers.com','plus.unsplash.com', 'cdn.pixabay.com'],
    },
}
  

export default nextConfig;
